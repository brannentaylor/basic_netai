# Align hostnames with docs/inventory/it-assets.yaml and network_lab inventory YAML.
all:
  children:
    csr_lab:
      hosts:
        csr01:
          ansible_host: 10.0.0.20
        csr02:
          ansible_host: 10.0.0.23
        csr03:
          ansible_host: 10.0.0.22
      vars:
        ansible_network_os: ios
