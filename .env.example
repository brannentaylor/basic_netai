# Copy values into your shell or a local `.env` (gitignored).

# Absolute path or relative to current working directory.
LAB_INVENTORY_PATH=src/network_lab/inventory/inventory.example.yaml

CSR_SSH_USERNAME=cisco
CSR_SSH_PASSWORD=replace-me
