# SNMP / streaming telemetry (placeholder)

Future work:

- SNMPv3 user per CSR, poll OSPF neighbor + interface tables.
- Model-driven telemetry (gRPC) if IOS-XE image supports it for your lab.

Document **what “green” means** before wiring automation (neighbor count, trap targets, etc.).
